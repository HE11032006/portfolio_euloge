"use client";
import { use } from "react";
import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft } from "lucide-react"; import { HoverBorderGradient } from "@/components/ui/hover-border-gradient"; import { ShutterTitle } from "@/components/ui/shutter-title"; import type { BlogPost } from "@/lib/content-types"; import postData from "../../../../content/blog-posts.json"; import { useLocale } from "@/components/locale-provider"; import { localized } from "@/lib/i18n";
const posts=postData as BlogPost[];
export default function ArticlePage({params}:{params:Promise<{slug:string}>}){const {locale,t,href}=useLocale();const {slug}=use(params);const post=posts.find(p=>p.slug===slug);if(!post)return notFound();return <div className="surface-dark text-[#ececec] min-h-screen overflow-x-hidden"><div className="writing-article-layout page-start"><aside className="writing-article-back"><Link href={href("/writing")} className="inline-flex"><HoverBorderGradient as="span" containerClassName="certs__gradient-btn" className="certs__gradient-content" duration={1.8}><ArrowLeft/><span>{t.common.back}</span></HoverBorderGradient></Link></aside><article className="w-full max-w-3xl pb-24"><header className="mb-10"><div className="flex flex-wrap items-center gap-3 text-[11px] font-mono tracking-wider uppercase mb-6 text-[#8e8e93]"><Link href={href("/writing")} className="hover:text-white transition-colors">{t.writing.title}</Link><span>/</span><span className="text-white">{localized(post.category,locale)}</span></div><div className="flex flex-wrap items-center gap-3 mb-6"><span className="bg-[#f2f0ec] text-black font-mono font-bold text-[11px] px-2.5 py-1 tracking-wider uppercase rounded-sm">{localized(post.category,locale)}</span><span className="text-xs font-mono text-[#8e8e93] uppercase">{localized(post.date,locale)}</span><span className="text-xs font-mono text-[#8e8e93] uppercase">{localized(post.readTime,locale)}</span></div><h1 className="text-3xl sm:text-5xl md:text-[52px] font-black text-white leading-[1.05] tracking-tight mb-6 uppercase"><ShutterTitle text={localized(post.title,locale)}/></h1><p className="text-lg sm:text-xl text-neutral-400 font-light leading-relaxed">{localized(post.shortDescription,locale)}</p></header><img src={post.coverImage} alt="" className="my-10 aspect-[16/9] w-full rounded-lg object-cover"/><section className="text-neutral-300 text-[16px] sm:text-[17px] leading-[1.75] space-y-10 max-w-[720px] mx-auto pt-6">{post.sections.map((section,index)=><div key={index}>{section.heading&&<h2 className="text-xl sm:text-2xl font-bold text-white mb-4 tracking-tight">{localized(section.heading,locale)}</h2>}<p>{localized(section.body,locale)}</p>{section.image&&<img src={section.image} alt="" className="mt-6 w-full rounded-lg object-cover"/>}</div>)}</section></article></div></div>}
